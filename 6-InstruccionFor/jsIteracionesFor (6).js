function mostrar()
{
    var numero
    var numPar
    numero = parseInt(prompt("Ingrese un numero"));
    for (;  ;)
    {
        numero = parseInt(prompt("Ingrese un numero"));
        if (numero % 2 == 0)
		{
			numPar++;
		}
    }



}//FIN DE LA FUNCIÓN